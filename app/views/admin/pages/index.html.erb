<h1>page list</h1>
<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th
    </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.title %>
    <td><%= link_to 'Show' , page_path(page.slug) %></td>
    <td><%= link_to 'Edit' , edit_admin_page_path(page) %></td>
    <td><%= link_to 'Destory', admin_page_path(page) , :confirm => 'Are you sure?' , :method => 'delete' %></td>
  </tr>
<% end %>
</table>
<%= link_to 'New Page' , new_admin_page_path %>
